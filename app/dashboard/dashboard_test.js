'use strict';

describe('Minion.dashboard module', function() {

  beforeEach(module('myApp.view2'));

  describe('dashboard controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var DashboardIndexCtrl = $controller('DashboardIndexCtrl');
      expect(DashboardIndexCtrl).toBeDefined();
    }));

  });
});
