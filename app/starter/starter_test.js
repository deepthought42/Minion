'use strict';

describe('Minion.starter module', function() {

  beforeEach(module('Minion.starter'));

  describe('starter controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var StarterIndexCtrl = $controller('StarterIndexCtrl');
      expect(StarterIndexCtrl).toBeDefined();
    }));

  });
});
