'use strict';

describe('Minion.domain module', function() {

  beforeEach(module('myApp.view2'));

  describe('domain controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var DashboardIndexCtrl = $controller('DomainCtrl');
      expect(DashboardIndexCtrl).toBeDefined();
    }));

  });
});
